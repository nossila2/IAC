name: CI/CD for react Todo 

on:
  push:
    branches:
      - main

jobs:
  # Build Job
  build:
    runs-on: default-self-hosted
    steps:
      - name: Build Project
        run: |
          hostname
         
