name: CI/CD for react Todo 

on:
  push:
    branches:
      - main

jobs:
  # Build Job
  build:
    runs-on: ubuntu-s-1vcpu-512mb-10gb-nyc1-01
    steps:
      - name: Build Project
        run: |
          hostname
         
